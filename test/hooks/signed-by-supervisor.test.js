const feathers = require('@feathersjs/feathers');
const signedBySupervisor = require('../../src/hooks/signed-by-supervisor');

describe('\'signed-by-supervisor\' hook', () => {
  let app;

  beforeEach(() => {
    app = feathers();

    app.use('/dummy', {
      async get(id) {
        return { id };
      }
    });

    app.service('dummy').hooks({
      after: signedBySupervisor()
    });
  });

  it('runs the hook', async () => {
    expect.assertions(1);
    const result = await app.service('dummy').get('test');
    expect(result).toEqual({ id: 'test' });
  });
});
